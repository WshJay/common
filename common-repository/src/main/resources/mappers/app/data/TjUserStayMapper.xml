<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xxx.common.repository.dao.data.TjUserStayDao" > 
	<resultMap id="TjUserStayMap" type="TjUserStay">
	    <result property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="deviceNo" column="device_no"/>
        <result property="deviceType" column="device_type"/>
        <result property="mac" column="mac"/>
        <result property="version" column="version"/>
        <result property="channel" column="channel"/>
        <result property="type" column="type"/>
        <result property="position" column="position"/>
        <result property="intoTime" column="into_time"/>
        <result property="leaveTime" column="leave_time"/>
        <result property="stayLong" column="stay_long"/>
    </resultMap>
   	
   	<!-- 所有查询字段 -->
   	<sql id="allSelectionColumn">
		id,
		user_id,
		device_no,
		device_type,
		mac,
		version,
		channel,
		type,
		position,
		into_time,
		leave_time,
		stay_long
	</sql>
   	
   	<!-- 根据ID查询实体数据 -->
    <select id="loadById" parameterType="int" resultMap="TjUserStayMap">
        SELECT <include refid="allSelectionColumn"/> FROM tj_user_stay WHERE id = #{value} LIMIT 1
    </select>

    <!-- 添加数据 -->
    <insert id="insert" parameterType="TjUserStay">
		<selectKey resultType="int" order="AFTER" keyProperty="id">
		SELECT LAST_INSERT_ID() AS id
		</selectKey>
        INSERT INTO tj_user_stay (
		 user_id, 
		 device_no, 
		 device_type, 
		 mac, 
		 version, 
		 channel, 
		 type, 
		 position, 
		 into_time, 
		 leave_time, 
		 stay_long
		 )  VALUES (
		 #{userId}, 
		 #{deviceNo}, 
		 #{deviceType}, 
		 #{mac}, 
		 #{version}, 
		 #{channel}, 
		 #{type}, 
		 #{position}, 
		 #{intoTime}, 
		 #{leaveTime}, 
		 #{stayLong})
    </insert>
    
    <!-- 根据ID删除数据-->
    <delete id="deleteById" parameterType="int">
        DELETE FROM  tj_user_stay WHERE id=#{value}
    </delete>
	
    <!-- 批量添加数据 -->
    <insert id="insertList" parameterType="java.util.List">
        INSERT INTO tj_user_stay (
		 id, 
		 user_id, 
		 device_no, 
		 device_type, 
		 mac, 
		 version, 
		 channel, 
		 type, 
		 position, 
		 into_time, 
		 leave_time, 
		 stay_long
		 )  VALUES 
		 <foreach collection="list" item="item" index="index" separator=",">
		(
		 #{item.id}, 
		 #{item.userId}, 
		 #{item.deviceNo}, 
		 #{item.deviceType}, 
		 #{item.mac}, 
		 #{item.version}, 
		 #{item.channel}, 
		 #{item.type}, 
		 #{item.position}, 
		 #{item.intoTime}, 
		 #{item.leaveTime}, 
		  #{item.stayLong})
		 </foreach>		 
    </insert>
    
    <select id="selectMaxIntoTime"  parameterType="TjUserStay" resultType="java.util.Date">
    	select max(into_time) from tj_user_stay where  device_no= #{deviceNo} and type=#{type}
    </select>
    
    <!-- 获取最大的插入时间 -->
    <select id="loadMaxIntoTime" parameterType="TjUserStay" resultMap="TjUserStayMap">
    	SELECT id, into_time FROM tj_user_stay 
    	WHERE device_no= #{deviceNo} AND type=#{type} ORDER BY into_time desc LIMIT 1
    </select>
    
    <!-- 更新设备停留时长 -->
    <update id="updateStayLongById">
    	update tj_user_stay set leave_time=#{leaveTime},stay_long=#{stayLong} where id = #{id}
    </update>
</mapper>  