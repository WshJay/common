<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xxx.common.repository.dao.data.TjWidgetClickDao" > 
	<resultMap id="TjWidgetClickMap" type="TjWidgetClick">
	    <result property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="widgetId" column="widget_id"/>
        <result property="widgetName" column="widget_name"/>
        <result property="targetId" column="target_id"/>
        <result property="type" column="type"/>
        <result property="position" column="position"/>
        <result property="deviceNo" column="device_no"/>
        <result property="deviceType" column="device_type"/>
        <result property="createTime" column="create_time"/>
    </resultMap>
   	
   	<!-- 所有查询字段 -->
   	<sql id="allSelectionColumn">
		id,
		user_id,
		widget_id,
		widget_name,
		target_id,
		type,
		position,
		device_no,
		device_type,
		create_time
	</sql>
   	
   	<!-- 根据ID查询实体数据 -->
    <select id="loadById" parameterType="int" resultMap="TjWidgetClickMap">
        SELECT <include refid="allSelectionColumn"/> FROM tj_widget_click WHERE id = #{value} LIMIT 1
    </select>

    <!-- 添加数据 -->
    <insert id="insert" parameterType="TjWidgetClick">
		<selectKey resultType="int" order="AFTER" keyProperty="id">
		SELECT LAST_INSERT_ID() AS id
		</selectKey>
        INSERT INTO tj_widget_click (
		 user_id, 
		 widget_id, 
		 widget_name, 
		 target_id, 
		 type, 
		 position, 
		 device_no, 
		 device_type, 
		 create_time,
		 city_id,
		 city_name
		 )  VALUES (
		 #{userId}, 
		 #{widgetId}, 
		 #{widgetName}, 
		 #{targetId}, 
		 #{type}, 
		 #{position}, 
		 #{deviceNo}, 
		 #{deviceType}, 
		 #{createTime},
		 #{cityId},
		 #{cityName})
    </insert>
    
    <!-- 根据ID删除数据-->
    <delete id="deleteById" parameterType="int">
        DELETE FROM  tj_widget_click WHERE id=#{value}
    </delete>
	
    <!-- 批量添加数据 -->
    <insert id="insertList" parameterType="java.util.List">
        INSERT INTO tj_widget_click (
		 id, 
		 user_id, 
		 widget_id, 
		 widget_name, 
		 target_id, 
		 type, 
		 position, 
		 device_no, 
		 device_type, 
		 create_time
		 )  VALUES 
		 <foreach collection="list" item="item" index="index" separator=",">
		(
		 #{item.id}, 
		 #{item.userId}, 
		 #{item.widgetId}, 
		 #{item.widgetName}, 
		 #{item.targetId}, 
		 #{item.type}, 
		 #{item.position}, 
		 #{item.deviceNo}, 
		 #{item.deviceType}, 
		  #{item.createTime})
		 </foreach>		 
    </insert>
</mapper>  